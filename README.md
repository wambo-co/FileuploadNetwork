# FileUploadNetwork
##
Start the Project
```
npm install bootstrap-icons-font
npm install vendor
brew install composer
composer install
composer update

```

- Features:
  CMS / Registration & Login with PHP & Mysql Database 

